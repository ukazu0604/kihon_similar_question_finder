# 自動テストの概要

このドキュメントは、プロジェクトに実装されている自動テスト（E2Eテスト）の内容を説明します。
テストは `pytest` と `Selenium` を使用して実行されます。

**テストの実行方法**: プロジェクトのルートディレクトリで `py run_all_tests.py` を実行します。

---

## テストスイート: `test_main_page.py`

このファイルには、アプリケーションの主要な機能に関するテストが含まれています。

### 1. `test_content_is_rendered`

- **目的**: アプリケーション起動時に、データ（JSON）が正しく読み込まれ、トップページのカテゴリ一覧が描画されることを確認します。
- **検証内容**:
  - トップページにアクセスした際、`.middle-category-item` というクラス名を持つカテゴリ項目が1つ以上表示されること。

### 2. `test_no_severe_console_errors`

- **目的**: アプリケーションの動作中に、ブラウザのコンソールに深刻な（`SEVERE`レベルの）JavaScriptエラーが発生していないことを確認します。
- **検証内容**:
  - トップページを読み込んだ後、ブラウザのコンソールログに `SEVERE` レベルのエラーが存在しないこと。

### 3. `test_untouched_filter_does_not_hide_on_check`

- **目的**: 「未着手のみ表示」フィルターの動作が、ユーザーにとって直感的なものであることを保証します。具体的には、問題にチェックを入れても即座にリストから消えず、リストが更新されるタイミングで非表示になることを確認します。
- **検証内容**:
  1. テスト開始時にチェック状態をリセットします。
  2. 詳細ページで「未着手のみ表示」をオンにします。
  3. 問題にチェックを入れた直後、リストからその問題が消えていない（＝問題数が減っていない）ことを確認します。
  4. ソート順を変更するなどしてリストを再描画させた後、問題がリストから消えている（＝問題数が1つ減っている）ことを確認します。