# 自動テストの概要

このドキュメントは、プロジェクトに実装されている自動テスト（E2Eテスト）の内容を説明します。
テストは `pytest` と `Selenium` を使用して実行されます。

**テストの実行方法**: プロジェクトのルートディレクトリで `py run_all_tests.py` を実行します。

---

## テストスイート: `test_main_page.py`

基本的なページ描画の確認を行います。

### 1. `test_content_is_rendered`
- **目的**: アプリケーション起動時に、JSONデータが読み込まれ、トップページのカテゴリ一覧が描画されることを確認します。

### 2. `test_no_severe_console_errors`
- **目的**: ブラウザのコンソールに `SEVERE` レベルのエラーが出ていないことを確認します。

### 3. `test_untouched_filter_does_not_hide_on_check`
- **目的**: 「未着手のみ表示」フィルターがONの状態でチェックを入れても、即座に非表示にならず、リスト更新時に非表示になる挙動を確認します。

---

## テストスイート: `test_browser_console.py`

ブラウザコンソールのエラーチェックに特化したテストです。

### 1. `test_no_console_errors`
- **目的**: ページロード後にコンソールログを取得し、`SEVERE` エラーがあればテストを失敗させます。`WARNING` レベルのログは出力のみ行います。

---

## テストスイート: `test_ui_interactions.py`

ユーザーインターフェースの操作と状態管理（永続化）に関する包括的なテストです。

### 1. `test_navigation_to_detail_and_back`
- **目的**: カテゴリ一覧から詳細ページへの遷移と、戻るボタンでの復帰が正しく動作することを確認します。

### 2. `test_sort_order_persistence`
- **目的**: 並び順（ソート）を変更した後、ページをリロードしても設定が維持されていることを確認します。

### 3. `test_reaction_buttons_persistence`
- **目的**: 「推し」「いいね」「恐怖」ボタンをクリックしてカウントが増えること、およびリロード後もカウントが維持されることを確認します。

### 4. `test_checkbox_progress`
- **目的**: 問題のチェックボックスをクリックすると、トップページの進捗バーが更新されることを確認します。

### 5. `test_accordion_toggle`
- **目的**: 詳細ページ内の「類似問題」アコーディオンをクリックして、開閉動作が正しく行われることを確認します。

### 6. `test_large_category_accordion`
- **目的**: トップページの大項目（例：1.基礎理論）のアコーディオンを開閉し、その状態がリロード後も維持されることを確認します。

### 7. `test_untouched_filter`
- **目的**: 「未着手のみ表示」フィルターをONにすると、チェック済みの問題がリストから非表示になることを確認します。

### 8. `test_review_highlight_logic`
- **目的**: 復習タイミング（例：1時間後）が到来した問題がハイライト表示されるかを確認します。
- **手法**: `localStorage` に過去のタイムスタンプを持つデータを直接注入し、リロード後に `needs-review` クラスが付与されているかを検証します。

### 9. `test_reset_storage`
- **目的**: 「チェック状態をリセット」ボタンを押した際、確認ダイアログを経て全てのデータ（チェック、リアクション）が初期化されることを確認します。

---

## テストスイート: `test_mobile_view.py`

モバイルデバイスでの表示・動作を確認するテストです。

### 1. `test_mobile_link_conversion`
- **目的**: モバイル環境（iPhone X相当のUser-Agent）でアクセスした際、問題へのリンクがスマートフォン向けURL（`/s/`を含む）に書き換わっているかを確認します。

---

## テストスイート: `test_data_integrity.py`

表示データの整合性を確認するテストです。

### 1. `test_similarity_threshold`
- **目的**: 類似問題リストに表示されている問題の類似度が、閾値（50%）以上であることを確認します。

### 2. `test_reference_count_display`
- **目的**: 類似問題のメタ情報として、「被参照: X回」という形式でカウントが表示されているかを確認します。